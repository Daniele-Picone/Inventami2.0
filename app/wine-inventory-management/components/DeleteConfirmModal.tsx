"use client";
import {useState} from 'react';import type {Wine} from '@/data/wineData';import {AlertTriangle,Loader2,X} from 'lucide-react';
export default function DeleteConfirmModal({wine,onConfirm,onClose}:{wine:Wine;onConfirm:()=>void;onClose:()=>void}){const [loading,setLoading]=useState(false);return <div className="fixed inset-0 z-50 flex items-center justify-center p-4 bg-foreground/30 backdrop-blur-sm" onClick={e=>e.target===e.currentTarget&&onClose()}><div className="bg-card rounded-2xl shadow-2xl w-full max-w-sm fade-in"><div className="px-6 pt-6 pb-4 flex items-start gap-4"><div className="w-10 h-10 rounded-full bg-red-100 flex items-center justify-center"><AlertTriangle size={18} className="text-red-600"/></div><div><h3 className="text-base font-bold">Elimina vino</h3><p className="text-sm text-muted-foreground mt-1">Stai per eliminare <span className="font-semibold text-foreground">{wine.nome}</span>. Questa azione è irreversibile.</p></div><button onClick={onClose} className="ml-auto"><X size={14}/></button></div><div className="px-6 pb-6 flex justify-end gap-3"><button onClick={onClose} className="px-4 py-2 text-sm font-semibold text-muted-foreground hover:bg-secondary rounded-lg">Annulla</button><button onClick={async()=>{setLoading(true);await new Promise(r=>setTimeout(r,300));onConfirm()}} className="flex items-center gap-2 px-4 py-2 bg-red-600 text-white text-sm font-semibold rounded-lg min-w-[110px] justify-center">{loading?<Loader2 size={14} className="animate-spin"/>:'Elimina Vino'}</button></div></div></div>}
